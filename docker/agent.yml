services:
  wazuh-agent:
    image: wazuh/wazuh-agent:4.14.5
    restart: always
    environment:
      - WAZUH_MANAGER_SERVER=127.0.0.1
      - WAZUH_REGISTRATION_PASSWORD=MySuperS3cr3t
    network_mode: host
